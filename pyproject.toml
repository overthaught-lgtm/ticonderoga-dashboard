[project]
name = "ticonderoga"
version = "0.1.0"
description = "Ticonderoga Systems Holdings"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "flask>=3.1.0",
    "gunicorn>=23.0.0"
]

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"